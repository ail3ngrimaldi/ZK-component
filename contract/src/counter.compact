pragma language_version 0.17;
import CompactStandardLibrary;

export ledger validationCount: Counter;

export circuit validateCondition(value: Field, threshold: Field): [Field] {
    validationCount.increment(1);
    return [1]; // Ahora [Field] acepta [Uint<1>] porque Uint es subtipo de Field
}